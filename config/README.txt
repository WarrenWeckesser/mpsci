Various configuration files

coveragerc:
    Configuration file for pytest-cov.
    In the top directory, run the test using (for example):

        pytest --cov=mpsci --cov-config=config/coveragerc --cov-report html
